image: node:8.11.4

variables:
  METEOR_ALLOW_SUPERUSER: 'true'

cache:
  paths:
    - 'node_modules'
    - '.meteor'
    - '~/.npm'
    - '~/.meteor'
    - '~/meteor'

stages:
  - test

before_script:
  - ./install_meteor.sh
  - meteor npm i

unit_tests:
  stage: test
  script:
    - meteor npm t
